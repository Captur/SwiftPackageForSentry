import XCTest
@testable import SwiftPackageForSentry

final class SwiftPackageForSentryTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(SwiftPackageForSentry().text, "Hello, World!")
    }
}
